{
  "routes": [
    {
      "method": "GET",
      "path": "/humints",
      "handler": "humint.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/humints/count",
      "handler": "humint.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/humints/:id",
      "handler": "humint.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/humints",
      "handler": "humint.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/humints/:id",
      "handler": "humint.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/humints/:id",
      "handler": "humint.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
